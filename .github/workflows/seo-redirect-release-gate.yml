name: SEO Release Gate

on:
  workflow_dispatch:
  push:
    branches:
      - "release/seo-redirect-foundation"
      - "seo/phase2-*"
    paths:
      - "src/lib/categories.ts"
      - "src/lib/marketplace.functions.ts"
      - "src/lib/product-seo.ts"
      - "src/lib/product-slug-redirect.ts"
      - "src/lib/product-seo-admin.functions.ts"
      - "src/routes/products.$id.tsx"
      - "src/routes/sitemap[.]xml.ts"
      - "src/routes/_authenticated/admin.academy.product-seo.tsx"
      - "src/routes/_authenticated/admin.academy.upload.tsx"
      - "src/components/marketplace/ProductCard.tsx"
      - "supabase/migrations/20260909062000_product_slug_redirects.sql"
      - "supabase/migrations/20260910162000_product_seo_metadata.sql"
      - "tests/integration/product-slug-redirect.test.ts"
      - "tests/integration/product-seo-brand.test.ts"
      - "tests/integration/product-canonical-list-links.test.ts"
      - "tests/integration/product-seo-phase2-source-contract.test.ts"
      - "tests/integration/seo-redirect-release-source-contract.test.ts"
      - "scripts/verify-seo-redirect-production.mjs"
      - ".github/workflows/seo-redirect-release-gate.yml"
  pull_request:
    branches:
      - main
    paths:
      - "src/lib/categories.ts"
      - "src/lib/marketplace.functions.ts"
      - "src/lib/product-seo.ts"
      - "src/lib/product-slug-redirect.ts"
      - "src/lib/product-seo-admin.functions.ts"
      - "src/routes/products.$id.tsx"
      - "src/routes/sitemap[.]xml.ts"
      - "src/routes/_authenticated/admin.academy.product-seo.tsx"
      - "src/routes/_authenticated/admin.academy.upload.tsx"
      - "src/components/marketplace/ProductCard.tsx"
      - "supabase/migrations/20260909062000_product_slug_redirects.sql"
      - "supabase/migrations/20260910162000_product_seo_metadata.sql"
      - "tests/integration/product-slug-redirect.test.ts"
      - "tests/integration/product-seo-brand.test.ts"
      - "tests/integration/product-canonical-list-links.test.ts"
      - "tests/integration/product-seo-phase2-source-contract.test.ts"
      - "tests/integration/seo-redirect-release-source-contract.test.ts"
      - "scripts/verify-seo-redirect-production.mjs"
      - ".github/workflows/seo-redirect-release-gate.yml"

permissions:
  contents: read

concurrency:
  group: seo-release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release-gate:
    name: SEO regression, typecheck and production build
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run SEO regressions
        run: |
          bunx vitest run \
            tests/integration/product-slug-redirect.test.ts \
            tests/integration/product-seo-brand.test.ts \
            tests/integration/product-canonical-list-links.test.ts \
            tests/integration/product-seo-phase2-source-contract.test.ts \
            tests/integration/seo-redirect-release-source-contract.test.ts

      - name: Validate production verifier
        run: |
          node --check scripts/verify-seo-redirect-production.mjs
          node scripts/verify-seo-redirect-production.mjs --help

      - name: Enforce safe migration boundaries
        shell: bash
        run: |
          set -euo pipefail
          test -f supabase/migrations/20260909062000_product_slug_redirects.sql
          test -f supabase/migrations/20260910162000_product_seo_metadata.sql
          if grep -Eiq 'update[[:space:]]+public\.marketplace_products|set[[:space:]]+slug[[:space:]]*=' supabase/migrations/20260909062000_product_slug_redirects.sql; then
            echo "ERROR: redirect foundation migration must not mutate product slugs"
            exit 1
          fi
          if grep -Eiq 'update[[:space:]]+public\.marketplace_products|insert[[:space:]]+into[[:space:]]+public\.marketplace_products|delete[[:space:]]+from[[:space:]]+public\.marketplace_products|set[[:space:]]+slug[[:space:]]*=' supabase/migrations/20260910162000_product_seo_metadata.sql; then
            echo "ERROR: Phase 2 migration must be schema-only"
            exit 1
          fi
          if find supabase/migrations -maxdepth 1 -type f \
            \( -iname '*slug*cleanup*' -o -iname '*slug*rename*' -o -iname '*slug*backfill*' \) \
            | grep -q .; then
            echo "ERROR: slug cleanup/rename/backfill must not be automatic"
            exit 1
          fi

      - name: Typecheck
        run: bunx tsgo --noEmit

      - name: Production build
        run: bun run build
